# Control-Plane Repair Receipt — 2026-09-01

## Trigger

Continuation/review cycles exposed several coupled failures: flattened product interpretation, stale cross-surface controller state, repeated chat archaeology, differently worded duplicate work, and too many overlapping external background lanes. The user explicitly requested one extensible system and one continuation path that can review the whole record when necessary but normally starts from durable/live authority.

## Repairs applied

- Re-established the anti-flattening rule: the complete accepted `SCOPE_LEDGER` remains authoritative; current focus/vertical integration is sequencing and proof only.
- Added `docs/MASTER_CONTINUITY_CONTROLLER.md` as the canonical boot/continuity/execution doctrine for stateful ChatGPT/N0TE work.
- Updated `docs/STARTUP_RECONCILIATION.md` and `docs/CONTROL_PLANE_EXTERNAL_BRIDGE.md` to route normal startup through the master controller and use whole-history archaeology only on explicit/diagnostic triggers.
- Added explicit semantic deduplication and anti-loop behavior: repeated wording maps to existing canonical outcomes; repeated agent failure triggers repair of execution/state/harness/tool/authority rather than harder reprompting.
- Preserved bounded whole-history assimilation through a Context-Archaeologist trigger so old conversations, Drive material, GitHub history, automations and current web/provider evidence can restore lost requirements without becoming the normal startup path.
- Reconciled TellMeN0TE Drive `START HERE` into one human entrypoint: `CONTINUE` routes to the canonical controller instead of another parallel master prompt.
- Consolidated the cloud automation topology into one active TellMeN0TE + N0TE Master Controller. Former standalone artist-ops, production-knowledge and standards watchers were disabled and their jobs became due/relevant subroutines of the master.
- Kept the N0TE Build Worker disabled/dormant as an on-demand executor rather than an independent scheduled construction loop.
- Reconciled `TELLMEN0TE_OS` `CONTROL_PLANE`, `governance/automation_registry.json` and `docs/EXTERNAL_AUTOMATION_REGISTRY.md` to the actual one-controller topology.
- Preserved repository-native GitHub governance verification and canonical construction selection as separate internal authority roles, not competing cloud assistants.
- Current ecosystem research was incorporated into the design: serious persistent-agent systems separate conversation/session memory, long-term/distilled memory, execution/checkpoint state and tool/workspace state rather than expecting one giant prompt to carry everything.

## Governance repair during this cycle

The first master-controller commits correctly failed exact-head construction governance because `docs/MASTER_CONTINUITY_CONTROLLER.md` had not been listed in the active receipt's exact allowed paths. That failure occurred across all three OS jobs at the construction-governance gate before deeper tests. The same cycle diagnosed the cause and updated `governance/active_receipt.json` to authorize exactly that document, preserving the existing PR #63 delivery lane rather than bypassing governance or creating a parallel PR.

## Authority after this receipt

- PR #63 remains the single active delivery lane for this context/retention/supervision work.
- Live PR head and exact-head CI are volatile evidence and must be fetched at startup rather than copied indefinitely into this document.
- `governance/active_receipt.json`, current repository handoff/state, `N0TE_PRODUCT_DB`, `TELLMEN0TE_OS` and `docs/MASTER_CONTINUITY_CONTROLLER.md` jointly define the current control boundary according to their respective authority domains.
- Historical chats, old PRs/branches and archived Drive material remain provenance/fallback evidence, not normal live authority.

## Next wake condition

Consume exact-head CI for the current PR #63 head. If governance exposes a same-class defect, repair that defect only. If the inherited Windows exact-launch lease regression remains after control-plane governance passes, repair it through the governed receipt. If all required exact-head checks pass, close the bounded increment according to canonical governance and globally reselect from the whole accepted product instead of continuing context machinery by adjacency.
